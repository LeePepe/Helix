# Design System Init Flow (Orchestrator)

Use separate subagents for each phase and load only the relevant reference:

1. Figma Rules Fetcher → `references/design-system-figma-rules.md`
2. Codebase Analyzer → `references/design-system-codebase-analysis.md`

Combine Figma rules and codebase analysis into a single `.github/design-system-guide.md` with the required structure.
